spring:
  flyway:
    enabled: true

logging.level:
  org.springframework.orm.jpa.JpaTransactionManager: DEBUG

eureka:
  client:
    enabled: false
stubrunner:
  stubs-mode: LOCAL

microservices:
  clients:
    fraud:
      url: http://localhost:8081
